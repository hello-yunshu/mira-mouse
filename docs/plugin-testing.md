<!-- SPDX-License-Identifier: CC-BY-SA-4.0 -->
# 插件测试

每个读写工作流都需要以下 Fixtures：成功、超时、畸形响应、超大响应、拔线、状态不匹配，以及未知字段保留。通过这些 Fixtures 只能称为 `fixture-verified`，不代表硬件证据。硬件记录必须标识型号、VID/PID、固件、连接方式、操作、回读（readback）、验证者、日期和结果。
