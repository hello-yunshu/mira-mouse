<!-- SPDX-License-Identifier: CC-BY-SA-4.0 -->
# Plugin Versioning

Plugin packages use SemVer. Manifest, tag, filename, lock entry, and release metadata must agree. Breaking manifest/DSL changes require a Plugin API major version. Existing same-version assets are immutable and must never be silently replaced.

